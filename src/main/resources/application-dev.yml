# dev

logging:
  level:
    ROOT: WARN
    org.ontbrowser: DEBUG

# Actuator
management:
  endpoint:
    env:
      show-values: always
  endpoints:
    enabled-by-default: true
    web:
      exposure:
        include: "*"
