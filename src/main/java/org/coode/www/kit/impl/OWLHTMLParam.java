package org.coode.www.kit.impl;

@Deprecated
public enum OWLHTMLParam {
    uri,
    action,
    clear,
    format,
    label,
    expression,
    query,
    syntax,
    content,
    type,  // used for find and for cloud
    input, // used for find
    ontology,
    name,
    expanded,
    property,
    value,
    section, // used to specify which doclet is returned in htmlfrag mode
    cls, // used for hierarchy
    parent,
    redirect,
    threshold, // cloud
    zoom, confirm
}
